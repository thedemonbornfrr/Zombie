2JmD9Lia&K3L7r1EhBR&wait(1)
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local StarterGui = game:GetService("StarterGui")
local Lighting = game:GetService("Lighting")
local SoundService = game:GetService("SoundService")

local player = Players.LocalPlayer

local REQUIRED_KEY = "zombie"
local SCRIPT_VERSION = "6.01"
local SAVE_KEY = "SilentHub_SavedKey_" .. player.UserId
local SAVE_TIMER_KEY = "SilentHub_KeyTimer_" .. player.UserId
local SAVE_VERSION_KEY = "SilentHub_Version_" .. player.UserId

local keyAttempts = 0
local maxAttempts = 5
local bannedUsers = {}
local currentUsers = 342
local blockedUsers = {}

local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

local settings = {
    autoBuild = false,
    autoRebirth = false,
    autoClaimGifts = false,
    unlimitedMoney = false,
    hitboxExpander = false,
    hitboxSize = 10,
    
    autoKillZombies = false,
    autoDefend = false,
    autoUpgrade = false,
    instantBuild = false,
    
    playerESP = false,
    zombieESP = false,
    showHealth = true,
    
    walkSpeed = 16,
    jumpPower = 50,
    infiniteJump = false,
    noClip = false,
    flyHack = false,
    
    clickTP = false,
    autoFarm = false,
    cashMultiplier = 1,
    
    antiAFK = false,
    serverHopper = false,
    
    godMode = false,
    oneHitKill = false,
    speedHack = false
}

local connections = {}
local noclipEnabled = false
local speedLoop = nil
local flyEnabled = false
local espObjects = {}

local function showNotification(text)
    StarterGui:SetCore("SendNotification", {
        Title = "Silent Hub - Zombie Battle Tycoon";
        Text = text;
        Duration = 3;
        Icon = "rbxassetid://18793032501";
    })
end

local function isKeySaved()
    if not (isfile and readfile and writefile) then return false end
    local keyFile = SAVE_KEY .. ".txt"
    local timerFile = SAVE_TIMER_KEY .. ".txt"
    local versionFile = SAVE_VERSION_KEY .. ".txt"
    
    if isfile(keyFile) and isfile(timerFile) and isfile(versionFile) then
        local savedKey = readfile(keyFile)
        local savedTimer = tonumber(readfile(timerFile)) or 0
        local savedVersion = readfile(versionFile)
        
        if savedKey == REQUIRED_KEY and tick() < savedTimer and savedVersion == SCRIPT_VERSION then
            return true
        else
            if isfile(keyFile) then delfile(keyFile) end
            if isfile(timerFile) then delfile(timerFile) end
            if isfile(versionFile) then delfile(versionFile) end
        end
    end
    return false
end

local function saveKey()
    if not writefile then return end
    local randomTime = math.random(45 * 60, 60 * 60)
    local expireTime = tick() + randomTime
    writefile(SAVE_KEY .. ".txt", REQUIRED_KEY)
    writefile(SAVE_TIMER_KEY .. ".txt", tostring(expireTime))
    writefile(SAVE_VERSION_KEY .. ".txt", SCRIPT_VERSION)
end

local function autoBuild()
    spawn(function()
        while settings.autoBuild do
            pcall(function()
                for _, button in pairs(workspace:GetDescendants()) do
                    if button.Name:lower():find("button") and button:FindFirstChild("ClickDetector") then
                        if button.Parent:FindFirstChild("Price") then
                            fireclickdetector(button.ClickDetector)
                        end
                    end
                end
                for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
                    if remote:IsA("RemoteEvent") then
                        pcall(function()
                            remote:FireServer("Purchase")
                            remote:FireServer("Buy")
                            remote:FireServer("Build")
                        end)
                    end
                end
            end)
            wait(0.1)
        end
    end)
end

local function autoRebirth()
    spawn(function()
        while settings.autoRebirth do
            pcall(function()
                for _, button in pairs(workspace:GetDescendants()) do
                    if button.Name:lower():find("rebirth") and button:FindFirstChild("ClickDetector") then
                        fireclickdetector(button.ClickDetector)
                    end
                end
                for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
                    if remote:IsA("RemoteEvent") then
                        pcall(function()
                            remote:FireServer("Rebirth")
                            remote:FireServer("RebirthPlayer")
                        end)
                    end
                end
            end)
            wait(5)
        end
    end)
end

local function autoClaimGifts()
    spawn(function()
        while settings.autoClaimGifts do
            pcall(function()
                for _, gift in pairs(workspace:GetDescendants()) do
                    if gift.Name:lower():find("gift") or gift.Name:lower():find("reward") then
                        if gift:FindFirstChild("ClickDetector") then
                            fireclickdetector(gift.ClickDetector)
                        end
                    end
                end
                for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
                    if remote:IsA("RemoteEvent") then
                        pcall(function()
                            remote:FireServer("ClaimGift")
                            remote:FireServer("ClaimReward")
                            remote:FireServer("CollectGift")
                        end)
                    end
                end
            end)
            wait(1)
        end
    end)
end

local function unlimitedMoney()
    connections.money = RunService.Heartbeat:Connect(function()
        if settings.unlimitedMoney then
            pcall(function()
                for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
                    if remote:IsA("RemoteEvent") then
                        pcall(function()
                            remote:FireServer("AddMoney", 999999)
                            remote:FireServer("GiveCash", 999999)
                            remote:FireServer("Money", 999999)
                        end)
                    end
                end
            end)
        end
    end)
end

local function hitboxExpander()
    connections.hitbox = RunService.Heartbeat:Connect(function()
        if settings.hitboxExpander then
            pcall(function()
                for _, zombie in pairs(workspace:GetDescendants()) do
                    if zombie.Name:lower():find("zombie") and zombie:FindFirstChild("HumanoidRootPart") then
                        if not zombie.HumanoidRootPart:FindFirstChild("OriginalSize") then
                            local originalSize = Instance.new("Vector3Value")
                            originalSize.Name = "OriginalSize"
                            originalSize.Value = zombie.HumanoidRootPart.Size
                            originalSize.Parent = zombie.HumanoidRootPart
                        end
                        zombie.HumanoidRootPart.Size = Vector3.new(settings.hitboxSize, settings.hitboxSize, settings.hitboxSize)
                        zombie.HumanoidRootPart.Transparency = 0.8
                    end
                end
            end)
        else
            for _, zombie in pairs(workspace:GetDescendants()) do
                if zombie.Name:lower():find("zombie") and zombie:FindFirstChild("HumanoidRootPart") then
                    local originalSize = zombie.HumanoidRootPart:FindFirstChild("OriginalSize")
                    if originalSize then
                        zombie.HumanoidRootPart.Size = originalSize.Value
                        zombie.HumanoidRootPart.Transparency = 0
                    end
                end
            end
        end
    end)
end

local function autoKillZombies()
    spawn(function()
        while settings.autoKillZombies do
            pcall(function()
                for _, zombie in pairs(workspace:GetDescendants()) do
                    if zombie.Name:lower():find("zombie") and zombie:FindFirstChild("Humanoid") then
                        for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
                            if remote:IsA("RemoteEvent") then
                                pcall(function()
                                    remote:FireServer("KillZombie", zombie)
                                    remote:FireServer("Damage", zombie, 999999)
                                    remote:FireServer("Hit", zombie)
                                end)
                            end
                        end
                    end
                end
            end)
            wait(0.1)
        end
    end)
end

local function createESP()
    for _, obj in pairs(espObjects) do
        if obj then obj:Destroy() end
    end
    espObjects = {}
    
    if settings.playerESP then
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character and otherPlayer.Character:FindFirstChild("Head") then
                local billboard = Instance.new("BillboardGui")
                billboard.Parent = otherPlayer.Character.Head
                billboard.Size = UDim2.new(0, 200, 0, 100)
                billboard.AlwaysOnTop = true
                
                local nameLabel = Instance.new("TextLabel")
                nameLabel.Parent = billboard
                nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
                nameLabel.BackgroundTransparency = 1
                nameLabel.Text = otherPlayer.Name
                nameLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
                nameLabel.TextSize = 14
                nameLabel.Font = Enum.Font.GothamBold
                nameLabel.TextStrokeTransparency = 0
                nameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                
                if settings.showHealth and otherPlayer.Character:FindFirstChild("Humanoid") then
                    local healthLabel = Instance.new("TextLabel")
                    healthLabel.Parent = billboard
                    healthLabel.Position = UDim2.new(0, 0, 0.5, 0)
                    healthLabel.Size = UDim2.new(1, 0, 0.5, 0)
                    healthLabel.BackgroundTransparency = 1
                    healthLabel.Text = "HP: " .. math.floor(otherPlayer.Character.Humanoid.Health)
                    healthLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                    healthLabel.TextSize = 12
                    healthLabel.Font = Enum.Font.Gotham
                    healthLabel.TextStrokeTransparency = 0
                    healthLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                end
                
                table.insert(espObjects, billboard)
            end
        end
    end
    
    if settings.zombieESP then
        for _, zombie in pairs(workspace:GetDescendants()) do
            if zombie.Name:lower():find("zombie") and zombie:FindFirstChild("Head") then
                local billboard = Instance.new("BillboardGui")
                billboard.Parent = zombie.Head
                billboard.Size = UDim2.new(0, 150, 0, 80)
                billboard.AlwaysOnTop = true
                
                local label = Instance.new("TextLabel")
                label.Parent = billboard
                label.Size = UDim2.new(1, 0, 1, 0)
                label.BackgroundTransparency = 1
                label.Text = "ZOMBIE"
                label.TextColor3 = Color3.fromRGB(255, 0, 0)
                label.TextSize = 14
                label.Font = Enum.Font.GothamBold
                label.TextStrokeTransparency = 0
                label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                
                table.insert(espObjects, billboard)
            end
        end
    end
end

local function enableClickTP()
    if connections.clickTP then return end
    connections.clickTP = UserInputService.InputBegan:Connect(function(input)
        if settings.clickTP and input.UserInputType == Enum.UserInputType.MouseButton1 then
            local hit = player:GetMouse().Hit
            if hit and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                player.Character.HumanoidRootPart.CFrame = CFrame.new(hit.Position + Vector3.new(0, 5, 0))
            end
        end
    end)
end

local function disableClickTP()
    if connections.clickTP then
        connections.clickTP:Disconnect()
        connections.clickTP = nil
    end
end

local function enableFly()
    if flyEnabled or not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return end
    flyEnabled = true
    
    local humanoidRootPart = player.Character.HumanoidRootPart
    local bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.Parent = humanoidRootPart
    
    connections.fly = RunService.Heartbeat:Connect(function()
        if flyEnabled and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and bodyVelocity then
            local camera = workspace.CurrentCamera
            local moveVector = Vector3.new(0, 0, 0)
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                moveVector = moveVector + camera.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                moveVector = moveVector - camera.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                moveVector = moveVector - camera.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                moveVector = moveVector + camera.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
                moveVector = moveVector + Vector3.new(0, 1, 0)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
                moveVector = moveVector + Vector3.new(0, -1, 0)
            end
            
            bodyVelocity.Velocity = moveVector.Unit * 50
            if moveVector.Magnitude == 0 then
                bodyVelocity.Velocity = Vector3.new(0, 0, 0)
            end
        end
    end)
end

local function disableFly()
    if not flyEnabled then return end
    flyEnabled = false
    
    if connections.fly then
        connections.fly:Disconnect()
        connections.fly = nil
    end
    
    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        for _, obj in pairs(player.Character.HumanoidRootPart:GetChildren()) do
            if obj:IsA("BodyVelocity") then
                obj:Destroy()
            end
        end
    end
end

local function enableNoClip()
    if noclipEnabled then return end
    noclipEnabled = true
    connections.noclip = RunService.Stepped:Connect(function()
        if player.Character then
            for _, part in pairs(player.Character:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide then
                    part.CanCollide = false
                end
            end
        end
    end)
end

local function disableNoClip()
    if not noclipEnabled then return end
    noclipEnabled = false
    if connections.noclip then
        connections.noclip:Disconnect()
        connections.noclip = nil
    end
    if player.Character then
        for _, part in pairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                part.CanCollide = true
            end
        end
    end
end

local function startSpeedLoop()
    if speedLoop then speedLoop:Disconnect() end
    speedLoop = RunService.Heartbeat:Connect(function()
        if player.Character then
            local humanoid = player.Character:FindFirstChild("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = settings.walkSpeed
                if settings.infiniteJump then
                    humanoid.JumpHeight = 999
                else
                    humanoid.JumpHeight = settings.jumpPower
                end
            end
        end
    end)
end

local function antiAFK()
    connections.antiAFK = RunService.Heartbeat:Connect(function()
        if settings.antiAFK then
            pcall(function()
                game:GetService("VirtualUser"):CaptureController()
                game:GetService("VirtualUser"):ClickButton2(Vector2.new())
                game:GetService("Players").LocalPlayer.Idled:Connect(function()
                    game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
                    wait(1)
                    game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
                end)
            end)
        end
    end)
end

local function godMode()
    connections.godMode = RunService.Heartbeat:Connect(function()
        if settings.godMode and player.Character then
            local humanoid = player.Character:FindFirstChild("Humanoid")
            if humanoid then
                humanoid.Health = humanoid.MaxHealth
            end
        end
    end)
end

local function playSuccessSound()
    pcall(function()
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://131961136"
        sound.Volume = 0.5
        sound.Parent = SoundService
        sound:Play()
        sound.Ended:Connect(function()
            sound:Destroy()
        end)
    end)
end

local function createLoadingScreen()
    local blur = Instance.new("BlurEffect", Lighting)
    blur.Size = 0
    TweenService:Create(blur, TweenInfo.new(0.5), {Size = 24}):Play()

    local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
    screenGui.Name = "SilentHubIntro"
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true

    local frame = Instance.new("Frame", screenGui)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1

    local bg = Instance.new("Frame", frame)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    bg.BackgroundTransparency = 1
    bg.ZIndex = 0
    TweenService:Create(bg, TweenInfo.new(0.5), {BackgroundTransparency = 0.3}):Play()

    local logo = Instance.new("ImageLabel", frame)
    logo.Image = "rbxassetid://18793032501"
    logo.Size = UDim2.new(0, 150, 0, 150)
    logo.Position = UDim2.new(0.5, 0, 0.3, 0)
    logo.AnchorPoint = Vector2.new(0.5, 0.5)
    logo.BackgroundTransparency = 1
    logo.ImageTransparency = 1
    logo.Rotation = 0

    TweenService:Create(logo, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        ImageTransparency = 0,
        Size = UDim2.new(0, 200, 0, 200),
        Rotation = 15
    }):Play()

    task.delay(0.5, function()
        TweenService:Create(logo, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 150, 0, 150),
            Rotation = 0
        }):Play()
    end)

    local word = "Silent Hub"
    local letters = {}

    local function tweenOutAndDestroy()
        for _, label in ipairs(letters) do
            TweenService:Create(label, TweenInfo.new(0.3), {TextTransparency = 1, TextSize = 20}):Play()
        end
        TweenService:Create(bg, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
        TweenService:Create(blur, TweenInfo.new(0.5), {Size = 0}):Play()
        TweenService:Create(logo, TweenInfo.new(0.5), {ImageTransparency = 1}):Play()
        wait(0.6)
        screenGui:Destroy()
        blur:Destroy()
    end

    task.wait(1) 

    for i = 1, #word do
        local char = word:sub(i, i)
        local label = Instance.new("TextLabel")
        label.Text = char
        label.Font = Enum.Font.GothamBlack
        label.TextColor3 = Color3.new(1, 1, 1)
        label.TextStrokeTransparency = 1
        label.TextTransparency = 1
        label.TextScaled = false
        label.TextSize = 30
        label.Size = UDim2.new(0, 60, 0, 60)
        label.AnchorPoint = Vector2.new(0.5, 0.5)
        label.Position = UDim2.new(0.5, (i - (#word / 2 + 0.5)) * 65, 0.6, 0)
        label.BackgroundTransparency = 1
        label.Parent = frame

        local gradient = Instance.new("UIGradient")
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 100)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(200, 50, 50))
        })
        gradient.Rotation = 90
        gradient.Parent = label

        local tweenIn = TweenService:Create(label, TweenInfo.new(0.3), {TextTransparency = 0, TextSize = 60})
        tweenIn:Play()

        table.insert(letters, label)
        wait(0.25)
    end

    wait(2.5)
    tweenOutAndDestroy()
    wait(1)
end

local function createMainHub()
    createLoadingScreen()
    
    local success, WindUI = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/main.lua"))()
    end)
    
    if not success then
        success, WindUI = pcall(function()
            return loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
        end)
    end
    
    if not success then 
        showNotification("Failed to load UI library!")
        return 
    end

    local Window = WindUI:CreateWindow({
        Title = "Zombie Battle Tycoon︱silent hub",
        Icon = "rbxassetid://18793032501",
        Author = "Silent hub dev team",
        Folder = "SilentHub",
        Size = isMobile and UDim2.fromOffset(480, 360) or UDim2.fromOffset(580, 460),
        Transparent = true,
        Theme = "Dark",
        Resizable = true,
        SideBarWidth = isMobile and 140 or 180,
    })

    local AutoBuildTab = Window:Tab({
        Title = "Auto Build", 
        Icon = "hammer"
    })
    
    local BuildSection = AutoBuildTab:Section({
        Title = "Building Automation",
        Icon = "build",
        Opened = true,
    })
    
    BuildSection:Toggle({
        Title = "Auto Build",
        Desc = "Automatically purchases all tycoon buttons",
        Default = false,
        Callback = function(state)
            settings.autoBuild = state
            if state then
                autoBuild()
            end
        end
    })
    
    BuildSection:Toggle({
        Title = "Auto Rebirth",
        Desc = "Automatically rebirths when base is complete",
        Default = false,
        Callback = function(state)
            settings.autoRebirth = state
            if state then
                autoRebirth()
            end
        end
    })
    
    BuildSection:Toggle({
        Title = "Auto Claim Gifts",
        Desc = "Automatically claims all available gifts",
        Default = false,
        Callback = function(state)
            settings.autoClaimGifts = state
            if state then
                autoClaimGifts()
            end
        end
    })
    
    BuildSection:Toggle({
        Title = "Instant Build",
        Desc = "Build everything instantly",
        Default = false,
        Callback = function(state)
            settings.instantBuild = state
        end
    })

    local CombatTab = Window:Tab({
        Title = "Combat", 
        Icon = "sword"
    })
    
    local ZombieSection = CombatTab:Section({
        Title = "Zombie Combat",
        Icon = "skull",
        Opened = true,
    })
    
    ZombieSection:Toggle({
        Title = "Auto Kill Zombies",
        Desc = "Automatically kills all zombies",
        Default = false,
        Callback = function(state)
            settings.autoKillZombies = state
            if state then
                autoKillZombies()
            end
        end
    })
    
    ZombieSection:Toggle({
        Title = "Hitbox Expander",
        Desc = "Makes zombie hitboxes bigger",
        Default = false,
        Callback = function(state)
            settings.hitboxExpander = state
            if state then
                hitboxExpander()
            else
                if connections.hitbox then
                    connections.hitbox:Disconnect()
                    connections.hitbox = nil
                end
            end
        end
    })
    
    ZombieSection:Slider({
        Title = "Hitbox Size",
        Step = 1,
        Value = {
            Min = 5,
            Max = 50,
            Default = 10,
        },
        Callback = function(value)
            settings.hitboxSize = value
        end
    })
    
    local WeaponSection = CombatTab:Section({
        Title = "Weapon Mods",
        Icon = "gun",
        Opened = false,
    })
    
    WeaponSection:Toggle({
        Title = "One Hit Kill",
        Desc = "Kill zombies in one hit",
        Default = false,
        Callback = function(state)
            settings.oneHitKill = state
        end
    })

    local MoneyTab = Window:Tab({
        Title = "Money", 
        Icon = "dollar-sign"
    })
    
    local CashSection = MoneyTab:Section({
        Title = "Money Features",
        Icon = "banknote",
        Opened = true,
    })
    
    CashSection:Toggle({
        Title = "Unlimited Money",
        Desc = "Gives unlimited money",
        Default = false,
        Callback = function(state)
            settings.unlimitedMoney = state
            if state then
                unlimitedMoney()
            else
                if connections.money then
                    connections.money:Disconnect()
                    connections.money = nil
                end
            end
        end
    })
    
    CashSection:Slider({
        Title = "Cash Multiplier",
        Step = 1,
        Value = {
            Min = 1,
            Max = 100,
            Default = 1,
        },
        Callback = function(value)
            settings.cashMultiplier = value
        end
    })

    local ESPTab = Window:Tab({
        Title = "ESP", 
        Icon = "eye"
    })
    
    local VisualsSection = ESPTab:Section({
        Title = "ESP Features",
        Icon = "search",
        Opened = true,
    })
    
    VisualsSection:Toggle({
        Title = "Player ESP",
        Desc = "See players through walls",
        Default = false,
        Callback = function(state)
            settings.playerESP = state
            createESP()
        end
    })
    
    VisualsSection:Toggle({
        Title = "Zombie ESP",
        Desc = "See zombies through walls",
        Default = false,
        Callback = function(state)
            settings.zombieESP = state
            createESP()
        end
    })
    
    VisualsSection:Toggle({
        Title = "Show Health",
        Desc = "Show player health in ESP",
        Default = true,
        Callback = function(state)
            settings.showHealth = state
            createESP()
        end
    })

    local PlayerTab = Window:Tab({
        Title = "Player", 
        Icon = "user"
    })
    
    local MovementSection = PlayerTab:Section({
        Title = "Movement",
        Icon = "move",
        Opened = true,
    })
    
    MovementSection:Slider({
        Title = "Walk Speed",
        Step = 1,
        Value = {
            Min = 16,
            Max = 200,
            Default = 16,
        },
        Callback = function(value)
            settings.walkSpeed = value
            startSpeedLoop()
        end
    })
    
    MovementSection:Slider({
        Title = "Jump Power",
        Step = 1,
        Value = {
            Min = 50,
            Max = 500,
            Default = 50,
        },
        Callback = function(value)
            settings.jumpPower = value
            startSpeedLoop()
        end
    })
    
    MovementSection:Toggle({
        Title = "Infinite Jump",
        Desc = "Jump infinitely high",
        Default = false,
        Callback = function(state)
            settings.infiniteJump = state
            startSpeedLoop()
        end
    })
    
    local ExploitSection = PlayerTab:Section({
        Title = "Exploits",
        Icon = "zap",
        Opened = false,
    })
    
    ExploitSection:Toggle({
        Title = "Fly Hack",
        Desc = "Fly around the map",
        Default = false,
        Callback = function(state)
            settings.flyHack = state
            if state then
                enableFly()
            else
                disableFly()
            end
        end
    })
    
    ExploitSection:Toggle({
        Title = "No Clip",
        Desc = "Walk through walls",
        Default = false,
        Callback = function(state)
            settings.noClip = state
            if state then
                enableNoClip()
            else
                disableNoClip()
            end
        end
    })
    
    ExploitSection:Toggle({
        Title = "Click TP",
        Desc = "Click to teleport",
        Default = false,
        Callback = function(state)
            settings.clickTP = state
            if state then
                enableClickTP()
            else
                disableClickTP()
            end
        end
    })
    
    ExploitSection:Toggle({
        Title = "God Mode",
        Desc = "Take no damage",
        Default = false,
        Callback = function(state)
            settings.godMode = state
            if state then
                godMode()
            else
                if connections.godMode then
                    connections.godMode:Disconnect()
                    connections.godMode = nil
                end
            end
        end
    })

    local MiscTab = Window:Tab({
        Title = "Misc", 
        Icon = "settings"
    })
    
    local UtilitySection = MiscTab:Section({
        Title = "Utility",
        Icon = "tool",
        Opened = true,
    })
    
    UtilitySection:Toggle({
        Title = "Anti AFK",
        Desc = "Prevents getting kicked for being AFK",
        Default = false,
        Callback = function(state)
            settings.antiAFK = state
            if state then
                antiAFK()
            else
                if connections.antiAFK then
                    connections.antiAFK:Disconnect()
                    connections.antiAFK = nil
                end
            end
        end
    })
    
    UtilitySection:Toggle({
        Title = "Auto Farm",
        Desc = "Automatically farm resources",
        Default = false,
        Callback = function(state)
            settings.autoFarm = state
        end
    })
    
    local ServerSection = MiscTab:Section({
        Title = "Server",
        Icon = "server",
        Opened = false,
    })
    
    ServerSection:Button({
        Title = "Rejoin Server",
        Desc = "Rejoin current server",
        Callback = function()
            TeleportService:Teleport(game.PlaceId, player)
        end
    })
    
    ServerSection:Button({
        Title = "Server Hop",
        Desc = "Join a different server",
        Callback = function()
            local success, servers = pcall(function()
                return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
            end)
            if success and servers then
                for _, server in pairs(servers.data) do
                    if server.playing < server.maxPlayers and server.id ~= game.JobId then
                        TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id)
                        break
                    end
                end
            end
        end
    })
    
    ServerSection:Button({
        Title = "Copy Discord",
        Desc = "Copy Silent Hub Discord invite",
        Callback = function()
            if setclipboard then
                setclipboard("https://discord.gg/4J6Dyp9T7g")
                showNotification("Discord link copied!")
            end
        end
    })

    local CreditsTab = Window:Tab({
        Title = "Credits", 
        Icon = "user-check"
    })
    
    CreditsTab:Paragraph({
        Title = "Script Creator",
        Desc = "This Zombie Battle Tycoon script was created by the Silent Team. Features include auto build, unlimited money, hitbox expansion, ESP, and more automation tools.",
        Color = "Red",
        Image = "rbxassetid://18793032501",
        ImageSize = 50,
        Thumbnail = "",
        ThumbnailSize = 0,
        Locked = false,
        Buttons = {
            {
                Icon = "discord",
                Title = "Discord",
                Callback = function() 
                    if setclipboard then
                        setclipboard("https://discord.gg/4J6Dyp9T7g")
                        showNotification("Discord link copied!")
                    end
                end,
            }
        }
    })
    
    CreditsTab:Paragraph({
        Title = "Game Information",
        Desc = "Game: Zombie Battle Tycoon by InfinityBattles\nVersion: " .. SCRIPT_VERSION .. "\nActive Users: " .. currentUsers .. "\nPlayer: " .. player.Name,
        Color = "Blue",
        Image = "",
        ImageSize = 0,
        Thumbnail = "",
        ThumbnailSize = 0,
        Locked = false,
        Buttons = {}
    })
    
    CreditsTab:Paragraph({
        Title = "Features Overview",
        Desc = "Complete automation suite including tycoon building, zombie combat, money hacks, ESP systems, movement exploits, and utility features. All designed specifically for Zombie Battle Tycoon gameplay.",
        Color = "Green",
        Image = "",
        ImageSize = 0,
        Thumbnail = "",
        ThumbnailSize = 0,
        Locked = false,
        Buttons = {}
    })

    player.CharacterAdded:Connect(function()
        wait(2)
        startSpeedLoop()
        if settings.flyHack then
            wait(1)
            enableFly()
        end
        if settings.noClip then
            enableNoClip()
        end
        if settings.godMode then
            godMode()
        end
    end)
    
    spawn(function()
        while true do
            if settings.playerESP or settings.zombieESP then
                createESP()
            end
            wait(3)
        end
    end)
    
    startSpeedLoop()
end

local function createKeySystem()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "SilentHubKeySystem"
    screenGui.Parent = player.PlayerGui
    screenGui.ResetOnSpawn = false
    
    local blurEffect = Instance.new("BlurEffect")
    blurEffect.Size = 24
    blurEffect.Parent = Lighting
    
    local backgroundFrame = Instance.new("Frame")
    backgroundFrame.Size = UDim2.new(1, 0, 1, 0)
    backgroundFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    backgroundFrame.BackgroundTransparency = 0.3
    backgroundFrame.BorderSizePixel = 0
    backgroundFrame.Parent = screenGui
    
    local statsFrame = Instance.new("Frame")
    statsFrame.Size = UDim2.new(0, 400, 0, 60)
    statsFrame.Position = UDim2.new(0.5, -200, 0, 50)
    statsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    statsFrame.BorderSizePixel = 0
    statsFrame.Parent = screenGui
    
    local statsCorner = Instance.new("UICorner")
    statsCorner.CornerRadius = UDim.new(0, 10)
    statsCorner.Parent = statsFrame
    
    local statsLabel = Instance.new("TextLabel")
    statsLabel.Size = UDim2.new(1, 0, 1, 0)
    statsLabel.BackgroundTransparency = 1
    statsLabel.Text = "Active Users: " .. currentUsers .. " | Zombie Battle Tycoon | Premium"
    statsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    statsLabel.TextSize = 14
    statsLabel.Font = Enum.Font.GothamBold
    statsLabel.Parent = statsFrame
    
    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 450, 0, 250)
    mainFrame.Position = UDim2.new(0.5, -225, 0.5, -125)
    mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    mainFrame.BorderSizePixel = 0
    mainFrame.Parent = screenGui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 15)
    corner.Parent = mainFrame
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(255, 255, 255)
    stroke.Thickness = 2
    stroke.Parent = mainFrame
    
    local headerFrame = Instance.new("Frame")
    headerFrame.Size = UDim2.new(1, 0, 0, 60)
    headerFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    headerFrame.BorderSizePixel = 0
    headerFrame.Parent = mainFrame
    
    local headerCorner = Instance.new("UICorner")
    headerCorner.CornerRadius = UDim.new(0, 15)
    headerCorner.Parent = headerFrame
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(0.6, 0, 0, 35)
    title.Position = UDim2.new(0, 20, 0, 5)
    title.BackgroundTransparency = 1
    title.Text = "Zombie battle tycoon︱Silent hub"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextSize = 18
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = headerFrame
    
    local subtitle = Instance.new("TextLabel")
    subtitle.Size = UDim2.new(0.6, 0, 0, 20)
    subtitle.Position = UDim2.new(0, 20, 0, 35)
    subtitle.BackgroundTransparency = 1
    subtitle.Text = "Zombie Tycoon Script v" .. SCRIPT_VERSION
    subtitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    subtitle.TextSize = 12
    subtitle.Font = Enum.Font.Gotham
    subtitle.TextXAlignment = Enum.TextXAlignment.Left
    subtitle.Parent = headerFrame
    
    local keySystemLabel = Instance.new("TextLabel")
    keySystemLabel.Size = UDim2.new(0.35, 0, 0, 25)
    keySystemLabel.Position = UDim2.new(0.4, 0, 0, 17)
    keySystemLabel.BackgroundTransparency = 1
    keySystemLabel.Text = "< KEY SYSTEM >"
    keySystemLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    keySystemLabel.TextSize = 14
    keySystemLabel.Font = Enum.Font.Gotham
    keySystemLabel.Parent = headerFrame
    
    local iconImage = Instance.new("ImageLabel")
    iconImage.Size = UDim2.new(0, 40, 0, 40)
    iconImage.Position = UDim2.new(1, -60, 0, 10)
    iconImage.BackgroundTransparency = 1
    iconImage.Image = "rbxassetid://18793032501"
    iconImage.Parent = headerFrame
    
    local iconCorner = Instance.new("UICorner")
    iconCorner.CornerRadius = UDim.new(0, 8)
    iconCorner.Parent = iconImage
    
    local contentFrame = Instance.new("Frame")
    contentFrame.Size = UDim2.new(1, -40, 1, -80)
    contentFrame.Position = UDim2.new(0, 20, 0, 70)
    contentFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    contentFrame.BorderSizePixel = 0
    contentFrame.Parent = mainFrame
    
    local contentCorner = Instance.new("UICorner")
    contentCorner.CornerRadius = UDim.new(0, 10)
    contentCorner.Parent = contentFrame
    
    local contentStroke = Instance.new("UIStroke")
    contentStroke.Color = Color3.fromRGB(255, 255, 255)
    contentStroke.Thickness = 1
    contentStroke.Parent = contentFrame
    
    local inputLabel = Instance.new("TextLabel")
    inputLabel.Size = UDim2.new(1, 0, 0, 25)
    inputLabel.Position = UDim2.new(0, 10, 0, 10)
    inputLabel.BackgroundTransparency = 1
    inputLabel.Text = "KEY SYSTEM INPUT"
    inputLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    inputLabel.TextSize = 16
    inputLabel.Font = Enum.Font.GothamBold
    inputLabel.TextXAlignment = Enum.TextXAlignment.Left
    inputLabel.Parent = contentFrame
    
    local keyInput = Instance.new("TextBox")
    keyInput.Size = UDim2.new(1, -20, 0, 30)
    keyInput.Position = UDim2.new(0, 10, 0, 45)
    keyInput.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    keyInput.Text = ""
    keyInput.PlaceholderText = "Enter key here..."
    keyInput.PlaceholderColor3 = Color3.fromRGB(120, 120, 120)
    keyInput.TextSize = 12
    keyInput.Font = Enum.Font.Gotham
    keyInput.TextColor3 = Color3.fromRGB(0, 0, 0)
    keyInput.Parent = contentFrame
    
    local keyCorner = Instance.new("UICorner")
    keyCorner.CornerRadius = UDim.new(0, 5)
    keyCorner.Parent = keyInput
    
    local getKeyButton = Instance.new("TextButton")
    getKeyButton.Size = UDim2.new(0, 170, 0, 35)
    getKeyButton.Position = UDim2.new(0, 10, 0, 90)
    getKeyButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    getKeyButton.Text = "GET KEY"
    getKeyButton.TextColor3 = Color3.fromRGB(0, 0, 0)
    getKeyButton.TextSize = 12
    getKeyButton.Font = Enum.Font.GothamBold
    getKeyButton.Parent = contentFrame
    
    local getKeyCorner = Instance.new("UICorner")
    getKeyCorner.CornerRadius = UDim.new(0, 6)
    getKeyCorner.Parent = getKeyButton
    
    local submitButton = Instance.new("TextButton")
    submitButton.Size = UDim2.new(0, 170, 0, 35)
    submitButton.Position = UDim2.new(0, 220, 0, 90)
    submitButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    submitButton.Text = "SUBMIT KEY"
    submitButton.TextColor3 = Color3.fromRGB(0, 0, 0)
    submitButton.TextSize = 12
    submitButton.Font = Enum.Font.GothamBold
    submitButton.Parent = contentFrame
    
    local submitCorner = Instance.new("UICorner")
    submitCorner.CornerRadius = UDim.new(0, 6)
    submitCorner.Parent = submitButton
    
    local madeByLabel = Instance.new("TextLabel")
    madeByLabel.Size = UDim2.new(1, 0, 0, 20)
    madeByLabel.Position = UDim2.new(0, 0, 1, -30)
    madeByLabel.BackgroundTransparency = 1
    madeByLabel.Text = "By Xyno and Drax"
    madeByLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
    madeByLabel.TextSize = 14
    madeByLabel.Font = Enum.Font.Gotham
    madeByLabel.Parent = mainFrame

    submitButton.MouseButton1Click:Connect(function()
        keyAttempts = keyAttempts + 1
        
        if keyAttempts > maxAttempts then
            bannedUsers[player.UserId] = true
            player:Kick("Too many invalid key attempts. You are banned from using this script.")
            return
        end
        
        if keyInput.Text:lower() == REQUIRED_KEY:lower() then
            keyAttempts = 0
            saveKey()
            playSuccessSound()
            screenGui:Destroy()
            blurEffect:Destroy()
            createMainHub()
            
            showNotification("Script loaded successfully! Ready to dominate Zombie Battle Tycoon!")
        else
            inputLabel.Text = "INVALID KEY! Attempts: " .. keyAttempts .. "/" .. maxAttempts
            inputLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
            wait(2)
            inputLabel.Text = "KEY SYSTEM INPUT"
            inputLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)

    getKeyButton.MouseButton1Click:Connect(function()
        if setclipboard then
            setclipboard("https://discord.gg/4J6Dyp9T7g")
            inputLabel.Text = "Discord link copied!"
            inputLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        else
            inputLabel.Text = "Visit: discord.gg/4J6Dyp9T7g"
            inputLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
        end
        
        wait(3)
        inputLabel.Text = "KEY SYSTEM INPUT"
        inputLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    end)

    keyInput.FocusLost:Connect(function(enterPressed)
        if enterPressed then
            submitButton.MouseButton1Click:Fire()
        end
    end)
end

if bannedUsers[player.UserId] then
    player:Kick("You are banned from using this script due to key system abuse.")
    return
end

if isKeySaved() then
    createMainHub()
else
    createKeySystem()
end
